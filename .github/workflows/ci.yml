name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: "20"

jobs:
  build:
    name: Build gateway
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Build
        run: pnpm build

  docker:
    name: Build Docker image
    runs-on: ubuntu-latest
    needs: build
    outputs:
      pushed: ${{ steps.image.outputs.push }}
      image_name: ${{ steps.image.outputs.name }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve image name and push flag
        id: image
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_PASSWORD: ${{ secrets.DOCKERHUB_PASSWORD }}
        run: |
          if [ "${{ github.event_name }}" = "push" ] && [ "${{ github.ref }}" = "refs/heads/main" ] && [ -n "$DOCKERHUB_USERNAME" ] && [ -n "$DOCKERHUB_PASSWORD" ]; then
            echo "name=${DOCKERHUB_USERNAME}/lenga-gateway" >> "$GITHUB_OUTPUT"
            echo "push=true" >> "$GITHUB_OUTPUT"
          else
            echo "name=lenga-gateway" >> "$GITHUB_OUTPUT"
            echo "push=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Log in to Docker Hub
        if: steps.image.outputs.push == 'true'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: ${{ steps.image.outputs.push == 'true' }}
          tags: |
            ${{ steps.image.outputs.name }}:latest
            ${{ steps.image.outputs.name }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy on server
    runs-on: ubuntu-latest
    needs: docker
    if: needs.docker.outputs.pushed == 'true'
    steps:
      - name: SSH - docker login, replace container
        uses: appleboy/ssh-action@v1.2.5
        env:
          IMAGE_TAG: ${{ secrets.IMAGE_TAG }}
          IMAGE_NAME: ${{ secrets.IMAGE_NAME }}
          CONTAINER_NAME: ${{ secrets.CONTAINER_NAME }}
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_PASSWORD: ${{ secrets.DOCKERHUB_PASSWORD }}
          DEPLOY_DOCKER_RUN_EXTRA_ARGS: ${{ vars.DEPLOY_DOCKER_RUN_EXTRA_ARGS }}
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          username: ${{ secrets.DEPLOY_USER }}
          port: ${{ secrets.DEPLOY_SSH_PORT || 22 }}
          envs: DOCKERHUB_USERNAME,DOCKERHUB_PASSWORD,IMAGE_NAME,IMAGE_TAG,CONTAINER_NAME,DEPLOY_DOCKER_RUN_EXTRA_ARGS
          script: |
            set -euo pipefail
            echo "$DOCKERHUB_PASSWORD" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
            docker pull "${IMAGE_NAME}:${IMAGE_TAG}"

            PREV_IMAGE=$(docker inspect --format='{{.Config.Image}}' "$CONTAINER_NAME" 2>/dev/null || true)

            docker stop "$CONTAINER_NAME" 2>/dev/null || true
            docker rm "$CONTAINER_NAME" 2>/dev/null || true
            docker run -d --name "$CONTAINER_NAME" -p 4000:4000 --restart unless-stopped \
              ${DEPLOY_DOCKER_RUN_EXTRA_ARGS:-} "${IMAGE_NAME}:${IMAGE_TAG}"

            KEEP_IMAGES="${IMAGE_NAME}:${IMAGE_TAG}"
            if [ -n "$PREV_IMAGE" ] && [ "$PREV_IMAGE" != "${IMAGE_NAME}:${IMAGE_TAG}" ]; then
              KEEP_IMAGES="$KEEP_IMAGES|$PREV_IMAGE"
            fi
            docker images "${IMAGE_NAME}" --format '{{.Repository}}:{{.Tag}}' \
              | grep -vE "^($KEEP_IMAGES)$" \
              | xargs -r docker rmi 2>/dev/null || true

            docker image prune -f 2>/dev/null || true
